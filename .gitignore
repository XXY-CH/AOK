# The upstream Linux checkout is a local nested repository. It is kept outside
# the AOK history and mounted from a case-sensitive volume on macOS.
kernel/linux/
.aok-linux-*.sparseimage
host/.build/
kernel/.build/
